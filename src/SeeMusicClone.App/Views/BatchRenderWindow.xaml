<Window x:Class="SeeMusicClone.App.Views.BatchRenderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Batch Render MIDI Files to Video"
        Height="500"
        Width="660"
        MinHeight="460"
        MinWidth="620"
        WindowStartupLocation="CenterOwner"
        Background="#0D0D0F"
        FontFamily="Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#171618"/>
        <SolidColorBrush x:Key="ControlBrush" Color="#242226"/>
        <SolidColorBrush x:Key="ControlHoverBrush" Color="#302D33"/>
        <SolidColorBrush x:Key="BorderBrush2" Color="#3A363C"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#4FC3F7"/>
        <SolidColorBrush x:Key="TextBrush" Color="#EEF4FA"/>
        <SolidColorBrush x:Key="MutedTextBrush" Color="#AAA3AD"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <Style x:Key="LabelText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Width" Value="150"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="{StaticResource ControlBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush2}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="13,7"/>
            <Setter Property="MinHeight" Value="32"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Root" Property="Background" Value="{StaticResource ControlHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Root" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="#071018"/>
            <Setter Property="MinWidth" Value="110"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#101012"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush2}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="#101012"/>
            <Setter Property="Foreground" Value="#111111"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush2}"/>
            <Setter Property="Padding" Value="6,5"/>
        </Style>

        <Style TargetType="Slider">
            <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,18">
            <TextBlock Text="Batch Render"
                       FontSize="20"
                       FontWeight="SemiBold"/>
            <TextBlock Text="Export every MIDI file in a folder as a video."
                       Foreground="{StaticResource MutedTextBrush}"
                       Margin="0,3,0,0"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Input folder" Style="{StaticResource LabelText}"/>
            <TextBox x:Name="InputFolderBox" Grid.Column="1" Margin="0,0,8,0"/>
            <Button Grid.Column="2" Content="Browse" Click="BrowseInput_Click"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Output folder" Style="{StaticResource LabelText}"/>
            <TextBox x:Name="OutputFolderBox" Grid.Column="1" Margin="0,0,8,0"/>
            <Button Grid.Column="2" Content="Browse" Click="BrowseOutput_Click"/>
        </Grid>

        <Grid Grid.Row="3" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="170"/>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Resolution" Style="{StaticResource LabelText}"/>
            <ComboBox x:Name="ResolutionCombo" Grid.Column="1" SelectedIndex="0" Margin="0,0,18,0">
                <ComboBoxItem Content="1920x1080"/>
                <ComboBoxItem Content="1280x720"/>
                <ComboBoxItem Content="3840x2160"/>
            </ComboBox>
            <TextBlock Grid.Column="2" Text="FPS" Style="{StaticResource LabelText}" Width="42"/>
            <ComboBox x:Name="FpsCombo" Grid.Column="3" Width="100" SelectedIndex="0" HorizontalAlignment="Left">
                <ComboBoxItem Content="30"/>
                <ComboBoxItem Content="60"/>
            </ComboBox>
        </Grid>

        <Grid Grid.Row="4" Margin="0,0,0,14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="220"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Note speed" Style="{StaticResource LabelText}"/>
            <Slider x:Name="SpeedSlider" Grid.Column="1" Minimum="80" Maximum="600" Value="140" Margin="0,0,10,0"/>
            <Border Grid.Column="2"
                    Background="#101012"
                    BorderBrush="{StaticResource BorderBrush2}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Padding="8,4"
                    MinWidth="52">
                <TextBlock Text="{Binding ElementName=SpeedSlider, Path=Value, StringFormat={}{0:0}}"
                           FontFamily="Consolas"
                           HorizontalAlignment="Center"/>
            </Border>
        </Grid>

        <Border Grid.Row="5"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush2}"
                BorderThickness="1"
                CornerRadius="8"
                Margin="0,0,0,12">
            <ListBox x:Name="LogList"
                     Background="Transparent"
                     BorderThickness="0"
                     Foreground="{StaticResource MutedTextBrush}"
                     Padding="8"/>
        </Border>

        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ProgressBar x:Name="Progress"
                         Grid.Column="1"
                         Width="240"
                         Height="18"
                         Margin="0,0,12,0"/>
            <Button x:Name="RenderButton"
                    Grid.Column="2"
                    Content="Render All"
                    Style="{StaticResource PrimaryButton}"
                    Click="RenderButton_Click"/>
        </Grid>
    </Grid>
</Window>
